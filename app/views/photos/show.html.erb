<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= image_tag @photo.image.url(:large), class: 'photo', id: @photo.id %>
  <%= @photo.description %>
</p>

<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
