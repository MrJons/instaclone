<div class='form-container col-xs-4 col-xs-offset-4'>

  <%= form_for [@photo, @comment] do |f| %>

    <% if @comment.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being posted:</h2>

        <ul>
        <% comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group center-block">
      <%= f.label :body, class: 'hidden' %>
      <%= f.text_area :body, placeholder: 'Comment...', class: 'form-control' %>
    </div>

    <div>
      <%= f.submit :post, class: 'btn btn-primary' %>
    </div>
  <% end %>

</div>
